import logo from "../assets/logos.png";
import {SearchIcon, HeaderIcon} from "../icon";

const Navbar = () => {
  return (
    <nav className="flex items-center justify-center p-4">
        <HeaderIcon  />
    </nav>
  );
};

export default Navbar;
